vector<int> HeapSort(Monticulo mont){
    vector<int> retorno;
    vector<int> aux;
    while(mont.getNodos() != 0){
        aux.push_back(mont.borrarRaiz());
    }
    for(unsigned int i{aux.size()}; i > 0; i--){
        retorno.push_back(aux.at(i-1));
    }
    return retorno;
}
